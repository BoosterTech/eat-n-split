import styled from "styled-components";

export const Element = styled.button`
  display: block;
  padding: 6px 11px;
  font-size: 0.8rem;
  font-weight: 600;
  border-radius: 5px;
  border: none;
  background-color: #f7a75e;
  margin-left: auto;
  margin-top: 20px;

  @media (hover: hover) {
    cursor: pointer;
  }
`;
